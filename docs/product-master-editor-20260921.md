# 产品报价 Web 编辑布局重构（2026-09-21）

## 已部署范围

- 以 FileMaker「产品报价」为参照：左侧产品标识、双语名称、审核、MOQ 和报价成本；右侧保留原 11 个栏目及其顺序。
- 原生 `产品编辑_WebViewer` 已移除仅含重复标题和提示的「顶部导航」部分，正文 WebViewer 与签名 URL 保留。状态提示统一由网页显示。
- Web 字段映射在 `frontend/src/components/productMasterLayout.ts`。完整字段视图逐项渲染后端注册表，包含 133 个非容器字段和 42 个容器；未知分组归入「系统与辅助字段」，没有因重新分类丢弃字段。
- 全部字段保留原字段名、类型、重复位置；计算/身份字段只读，价格由服务端权限过滤。当前 FileMaker 签名上下文提供的权限使界面可见 155 字段；其余 20 个价格字段受权限控制，未从注册表删除。权限集来源仍需与 FileMaker 的实际权限集核对，不能仅凭账号名提升权限。
- 产品照片按容器数字位置排序，提供缩略图、原图弹层、下载；说明书 PDF 可预览，其他文件可下载。二维码不作为产品主图。42 个位置可在完整字段页统一搜索。
- 当前产品首次读取可复制并校验完整快照；已有 Web 副本优先读取，FileMaker 不可用不影响该副本。预览使用独立只读 store，未启用任何产品写 API 或回写 worker。
- FileMaker 同站点容器 302 跳转已兼容；每一跳重新核对 HTTPS、主机、端口，跨域跳转不发送凭据。
- 全量迁移与当前产品加载共用导入服务，通过 PostgreSQL 产品锁协调。历史资产身份冲突仅阻止相关产品，保留原 ID，进入失败清单，不任意选取或删除。

## 验证结果

- 产品与容器测试 31 项通过，包括来源权限、持久快照读取、重复容器导入、资产 ID 冲突隔离、同源/跨源跳转。
- TypeScript 和 Vite 构建通过。
- 本地隔离页面验证了原栏目导航、完整字段、说明书搜索及窄窗口布局。
- 生产当前产品 `036-EX01`：133 个普通字段、1 个二维码文件；原产品照片为空。
- 生产多图样本 `MJ007-073-Set`（UUID `8ab4d5ff-fbbf-473b-918f-4237b37c2d7c`）：133 个普通字段，9 份文件均写入 COS 并回读校验摘要：6 张产品照片、1 张销售记录图片、二维码、选中文件容器。
- 已在 FileMaker 实际看到样本 SKU、名称、9 份文件计数及主图；「產品照片」栏目列出 6/10 个已占用位置。

## 发布与未完成项

服务器版本目录 `/opt/starrc-filemaker/releases/product-master-20260921`。
后端镜像 `starrc-backend:product-master-20260921-editor2`，前端最终镜像 `starrc-frontend:product-master-20260921-editor3`。
Compose 继续使用 `current/deploy/starrc/product-master.release.yml` 覆盖文件；预览 true、主库编辑 false、FileMaker 写入 false。

首次迁移原先因旧资产重复关联退出，没有导入产品。本次已修复冲突隔离与下载跳转，重新启动全量迁移。日志 `migration-editor.log`，报告 `/data/product-master-import-20260921.json`；启动和样本通过均不代表全量完成。

关联表中的 BOM、报价记录、销售、库存、开发任务和产能明细，仍在原 FileMaker 栏目查看/编辑。Web 保留这些栏目并明确说明归属；本次未复制关联表的业务编辑器，也未将其冒充为产品自身字段。

保存及回写仍待账号配置、原生入口锁定和 UUID 新建验证；DMS 尚未切换。历史文件不删除。

末轮原图弹层与恢复原产品的界面操作因 Mac 自动锁屏暂停，已请求用户解锁；未声称该步骤验收通过。停留记录为多图样本 MJ007-073-Set，未修改其业务字段。

## 编辑操作收敛（用户后续要求）

前端已改为仅保留一组「取消」「保存」主操作，移除新建、刷新、重复保存按钮及产品切换入口。顶部固定，滚动后仍可操作。取消无论是否有修改都二次确认；未确认则保留编辑，确认后调用现有 `StarRC_CloseWebViewer`，传入 `action=close` 和当前 UUID。确认后的关闭不再触发重复的未保存提示。保存/上传处理中禁用取消；关闭失败保留输入并提示错误。普通浏览器尝试关闭当前页面，被阻止时提示手动关闭。

对应前端镜像 `starrc-frontend:product-master-20260921-actions`。TypeScript、Vite 构建通过；后端及迁移进程未重启。

## 基础资料与 SKU 校验（本轮更新）

基础资料改为全宽首个页签，进入和切换产品时默认显示；移除固定左栏、重复大标题及上下文栏。页签采用单选语义与深绿色实色高亮，图片页签切出后恢复普通样式。仅保留保存和取消主操作。

SKU 保存时校验空值和同源重复值，去除首尾空白、忽略大小写比较，排除当前 UUID；事务锁防止并发占用同一 SKU。错误返回 SKU_REQUIRED / SKU_DUPLICATE，并在基础资料 SKU 输入框定位提示。历史迁移保留旧数据，已存在的重复 SKU 必须更正才能从 Web 保存。完整迁移且原生编辑锁定前，另行查询 FileMaker 未迁移记录；无法校验时拒绝保存并明确提示。完整导入标记仅在迁移全部成功后设置。

验证：相关后端测试 40 项通过；TypeScript、Vite 构建通过；1100px 本地页面验证默认基础资料、图片页签切换及空 SKU 错误提示。前后端镜像均为 `product-master-20260921-basics`，生产健康检查通过。原迁移重启续跑，日志 `migration-basics.log`。只读开关保持，未宣称保存已开放或全量迁移已完成。

## 原生选择控件与草稿试填

修改历史、全部字段、同步状态统一放入主 tab，移除独立工具栏和字段搜索框；主 tab 自动换行。普通已注册可写字段在预览模式允许本地草稿编辑，UUID、计算字段和系统只读字段仍锁定；保存、附件上传和回写权限未开启。

通过 FileMaker Data API 实际核对「产品报价」：審核/產品審核、Upload_Date/日历、Newitem 和 ShowStock/check、關聯編號/關聯產品編號，以及只读的有現貨。原生控件元数据按 5 分钟缓存，仅返回有读取权限的产品自身字段；大型值列表通过搜索弹窗分页读取。关联业务表控件未纳入产品字段编辑。

客户弹窗读取现有 OData 客戶目录，只返回关联 ID、客戶代號、客戶公司簡稱。实测当前产品 CU339 对应 036 / Enjoy Smile Co.；客户主键从 OData 复合键解析，不能用代号替代。名称和编号入口共用弹窗，选择后同时带入 Client 和 id_client 草稿。可按名称、代号或关联 ID 搜索，有加载、失败、空结果及分页状态。

42 项后端测试通过；TypeScript 和 Vite 构建通过。生产前后端镜像更新为 product-master-20260921-controls；健康检查通过；迁移续跑日志 migration-controls.log。保存开关维持关闭。

FileMaker WebViewer 实测通过：独立修改历史 tab、无字段搜索工具栏；审核下拉可用，客户弹窗加载 803 条真实记录，输入 036 后找到 CU339 / Enjoy Smile Co. 并可选回；MOQ 本地试填显示未保存状态，保存按钮仍禁用。测试草稿经取消确认丢弃，再打开干净编辑页；未提交产品数据。

## 按实际字段长度收紧布局

对已迁移约 5,100 条产品做只读字段统计：MOQ 最长 4 位（90% 为 1 位），数量/分类代码大多 1–4 位，修改人最长 8 字符、日期时间 19 字符；英文名称最长 87 字符，历史记录超过 7,000 字符。该统计仅覆盖已迁移样本，不代表全量完成。

据此采用稳定的 12 列排版：数字与已确认勾选项占 2 列，普通短文本 3 列，客户名称 4 列，产品名称 6 列，说明和记录整行；窄窗口自动换行。未按当前输入长度动态收缩，避免输入时跳动。審核移至右上角胶囊选择框，新產品标志移至同一区域；有現貨有值时显示状态标签。新產品保留实际值“新產品”，加入目錄保留“1”，不把 category 数字代码、空样本的應課稅、包含时间的 status 自动转换成是否值。

记录信息改为建档/修改人和时间的紧凑横向信息行，UUID 独立细行显示；原字段仍可在全部字段查看。此轮仅更新前端镜像 starrc-frontend:product-master-20260921-density；后端、迁移及保存开关不变。TypeScript 和 Vite 构建通过。

生产 FileMaker WebViewer 1100px 窗口目视验收：右上角未审核和新品控件可见，产品信息为四列短字段/双列名称，客户编号窄列；数字字段约为普通文本的 2/3 宽。建档日期、人及修改日期、人均处于同一行，UUID 在下一条细行。保存仍禁用，未变更任何产品值。

## 移除业务栏目

按用户确认，移除开发进度、產品BOM、產品BOM排序、產品庫存、銷售紀錄、紀錄檔案、產能計算 7 个 tab，以及 37 个专属业务字段（含销售记录照片 1–6）。统一显示过滤同时覆盖原 tab、全部字段、顶部状态和附件预览；完整数据与附件关联仍保留，保存不会因隐藏字段而清空已有值或附件。

用户明确保留基础资料共用的客户、编号、分类、规格、备注和建档/修改信息。共用字段在全部字段中归入基本资料，防止移除旧分组后遗漏。前端注册表显示范围由 175 缩至 138 个，仍叠加原有账号权限过滤。

TypeScript、Vite 构建通过，静态核对栏目和隐藏字段清单。仅前端发布为 starrc-frontend:product-master-20260921-tabs，后端及迁移保持运行。

## 审核位置与客户入口合并

审核状态从页面顶栏移至「产品信息」卡片标题右侧，沿用原 pm-product-status 胶囊下拉样式。客户姓名和编号改为只读展示区，只有一个「选择客户」按钮，共用原有搜索弹窗并成对更新草稿；全部字段页面也使用同样入口。按钮同时检查两个字段的可写权限。未改保存开关或后端。

TypeScript、Vite 构建通过；前端镜像 starrc-frontend:product-master-20260921-customer 已发布。

## 客户对齐与滚动加载

客户标签固定为与法文名称一致的 17px 行高，选择按钮独立定位，不再撑高标签行；展示区与左侧名称框同高并左对齐。弹窗结果按钮显式 align-items:flex-start，客户名称和编号靠左。

移除上一页/下一页。首批 50 条，接近列表底部追加下一批；请求中加锁避免重复触发，换搜索词后清空列表、重置滚动和游标，过期请求不覆盖新查询，追加结果按 ID 去重；失败保留已载入结果并提供重试。服务端原分页接口不变。TypeScript、Vite 构建通过，前端镜像 product-master-20260921-lazy 已发布。

FileMaker 实际验证通过：客户框与法文名称框上下边缘对齐；弹窗名称/编号左对齐；滚动条拖至底部后已显示从 50/803 增至 100/803；输入 036 后重置为 4/4 条并显示全部结果。未选择或提交新的客户，关闭弹窗回到基础资料。

## 六图与去除报价/二维码

产品照片仅显示 檔案 1 | 容器 至 檔案 6 | 容器，重复位置均为 1；卡片固定顺序命名产品图片 1–6，逐卡支持单图拖入、文件选择、替换、移除和点击预览。去掉图片置顶操作，避免顺序与容器位置混淆。预览模式使用本地 File 草稿和 Object URL，不上传服务器；正式保存仍禁用。启用写入后复用资产上传流程的 field/repetition 绑定，worker 按相同 slot 回写并校验。

报价参考与对应普通字段从编辑页和全部字段移除；有圖沒圖、img_url 移除。qrcode 及图片 7–10 标为 managed=false，后续导入跳过、API 不展示/不允许上传，worker 不回写或清空；注册元数据保留供真实基表覆盖预检。已有历史附件不物理删除，隐藏旧附件不会因提交省略而被删除。

43 项后端测试通过，新增六图精确位置与退出管理容器不导入测试。TypeScript、Vite 构建通过。前后端版本 product-master-20260921-photos。

## 十八图与独立规格书栏目

按最新要求，将产品照片固定为 檔案 1 | 容器 至 檔案 18 | 容器，按数字排序，保留逐位置拖入、替换和点击预览。恢复第 7–10 个容器的管理权限；第 19、20 个容器从编辑页隐藏，已有附件不删除。新增「产品规格书」tab，对应真实字段「產品規格書」「產品規格書2」，使用相同图片草稿与预览能力。保存开关仍关闭。

43 项后端测试通过，覆盖 18 个产品照片和两个规格书的精确容器位置；TypeScript 和 Vite 构建通过。前后端发布为 product-master-20260921-photos18，迁移续跑日志 migration-photos18.log。

生产数据库只读核查：pm_upload 独立保存 COS objectKey、摘要、文件类型等元数据，并有 product_id；文件本体在 COS。当前产品附件关联仍保存在 pm_product.assets JSONB，pm_revision 保存版本快照。尚无独立 pm_product_asset/pm_asset_version 关系及版本表，不能宣称已完成原计划中的关系表设计。

存量限制：import_product 对已导入 UUID 直接返回，恢复第 7–10 个容器管理不会自动补导此前跳过的文件。需要另外执行不覆盖 Web 现有资产、保留审计的存量对账与补导；全量迁移仍未验收完成。

## 业务资料字段与语义分区

只读核对真实「產品 資料_業務」布局和原生控件：类别 38 项、车子比例 28 项（1/24 重复）、车型 99 项、产品分类 74 项；审核、库存状态和显示库存也有原生列表/勾选配置。该布局元数据附带大量无关值列表，默认请求超时；因此导出精简原生选项快照 product_master_business_controls.json，只保留 7 个实际控件对应列表。刷新脚本 product_master_export_business_controls.py 可重新只读提取，部署新快照后生效。运行时保留产品报价控件读取，并以业务快照覆盖同名控件，去重空值和重复选项。

基础资料划分为产品信息、分类与适配、尺寸与重量、库存信息、报价信息、描述与备注。按本次新要求恢复产品自身库存/报价字段，原已移除的业务 tab 不恢复；尺寸、净毛重、彩盒和外箱长宽高及 CBM 集中呈现。仅显示账号有权查看的字段，计算/汇总字段继续只读，关联业务表不加入主表写入。

44 项后端测试、TypeScript、Vite 构建通过。发布镜像 product-master-20260921-business。保存仍关闭。

## 2026-09-22 本地前端更新发布

按用户要求发布本地修改后的编辑器前端，包含分组排版、折叠区域、尺寸组合输入、报价分组及图片展示重构。TypeScript、Vite 构建通过。前端镜像 product-master-20260922-editor；后端保持 product-master-20260921-business，仅重建前端，不重启迁移。保存与 FileMaker 回写开关仍关闭。旧部署配置备份为 backup/business-release-20260922.yml。此次为生产部署，未执行 Git 提交或推送。
